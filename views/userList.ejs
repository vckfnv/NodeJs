<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
		<title>User</title>
		<link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
	</head>
	<body>
		<div class="board-page">
			<div class="board">
				<p align="right"> <%=username%> 관리자 님 환영합니다.<button id="log_out">Logout</button><button id="btn_board_list">게시판 보기</button></p>
				<h1>유저 목록</h1>

				<!-- 검색 시작 -->
				<form id="searchKey" action="/users/list/search" method="POST">
					<div class="search" align="right">
						
						<input id='searchid' name="searchKeyword" type="text" value="<%=sk%>"/>
						
						<button id = 'btnSubmit2'>검색</button>
					</div>
				</form>
				<!-- 검색 종료 -->

				<table class="title">
					<tr>
						<th width="20%">번호</th>
						<th width="40%">아이디</th>
						<th width="20%">이름</th>
						<th width="20%">등록일</th>
					</tr>
				</table>
				<table class="content">
					<% if(status == 'OK') {%>
						<%if(data.length == 0) {%>
							<tr>
									<td colspan="4">데이터가 없습니다.</td>
								</tr>
						<%}else{%>
							<% data.forEach(function(item,index){ %>
								<tr>
									<td width="20%"><%= item.uid %></td>
									<td width="40%"><a href = "/users/update?uid=<%=item.uid%>"><%= item.login_id %></a></td>
									<td width="20%"><%= item.user_name %></td>
									<td width="20%"><%= item.cdate2 %></td>
								</tr>
							<% });%>
						<%}%>
					<% } else{%>

						<tr>
							<td colspan="4">에러 잠시후 다시,게시물이 없습니다.</td>
						</tr>
					<%}%>
				</table>
				<div align="right"><button id="btn_user_new">새 유저 등록</button></div>
			</div>
		</div>
		<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
		<script src="/javascripts/users.js"></script>
		
	</body>
</html>